# 信件分拣优先级引擎设计

面向「浮空岛邮政调度员」的纯函数分拣引擎。综合**加急等级、截止窗、目的地约束**对收件箱/积压邮件排序并给出可直接提交的调度建议；同一输入永远得到同一结果；人工覆盖只追加、可撤销，且任何覆盖都无法绕过硬规则。

代码位置：

| 文件 | 职责 |
| --- | --- |
| `server/priority.js` | 纯函数核心：评分、硬闸门、截止窗、覆盖台账、分拣主流程 |
| `server/priorityService.js` | 状态流转：在游戏状态上挂载/更新台账，生成快照 |
| `server/app.js` | 三个 HTTP 接口（只读分拣、追加覆盖、撤销覆盖） |
| `server/store.js` | 台账字段的存档校验与损坏迁移 |
| `server/tests/priority.test.js` | 引擎规则测试（17 项） |
| `server/tests/priority-api.test.js` | 接口/持久化测试（5 项） |

## 1. 硬规则（人工覆盖也不能绕行）

硬规则是**准入闸门**，在每次分拣时对「装入某信使的某封信」这一具体动作评估，与是否存在人工覆盖无关：

| 闸门 | 原因码 | 含义 |
| --- | --- | --- |
| 截止窗开放 | `WINDOW_NOT_OPEN` | 该信使今日窗口标记关闭或不存在 |
| 截止窗时间 | `WINDOW_CLOSED` | `currentHour >= window.closesAtHour`（默认规划时刻 6 点，7 点起航关窗） |
| 目的地合法 | `TARGET_FORBIDDEN` | 目标岛必须存在且不是天枢邮港 |
| 目的地一致 | （同上） | **建议目标永远等于信件 `recipientIslandId`，引擎不提供误投能力** |
| 邮件数量 | `LETTER_LIMIT` | 每信使 `maxLetters` 封顶 |
| 载重 | `WEIGHT_LIMIT` | 装入后总重 ≤ `capacity`（复用主引擎 0.001kg 容差） |
| 航时可行性 | `late` 标记 | 复用 `calculateRoute` 同款风况/天气模拟，预计逾时只做标记，不改变能否装入 |

关键性质：

- **置顶（PIN）撞上硬规则时拒绝并保留信件不自动改派**，拒绝原因进入 `refusedOverrides`，信件进入 `unassigned` 并带 `overrideRefused: true`。调度员必须显式撤销置顶，才会回到自动分配——不存在「置顶悄悄换条船」。
- **加权（BOOST）有 15 分上限**（`maxBoostPoints`），超限覆盖整条拒绝（`OVERRIDE_BOOST_OUT_OF_RANGE`），按自动分数处理，绝不静默截断；加权只改排名，装不下时硬规则照样拦截。
- **暂缓（HOLD）只对仍开放的信件生效**；已结算信件在写入阶段即被服务层拒绝。
- 截止窗是调用方**显式传入**的输入（`buildDefaultWindows(state, config)` 可生成默认值，也可传真实运营数据），引擎不偷偷读时钟。

## 2. 评分模型（整数分，全部可审计）

```
总分 = 加急分 + 截止窗分 + 积压老化分 + 目的地关系分 + 人工加权分
```

| 维度 | 规则 | 默认值 |
| --- | --- | --- |
| 加急 `urgencyPoints` | 1/2/3 级固定基础分 | 10 / 26 / 45 |
| 截止窗 `slackBands` | 距 `deadlineDay/Hour` 剩余小时数分档 | ≤0h:30，≤2h:22，≤5h:12，≤10h:5，其余 0 |
| 老化 `agingPointsPerDay/Cap` | 积压天数 × 单价，封顶 | 8/天，封顶 24 |
| 目的地关系 `relationPointsCap` | 发件岛↔收件岛关系 0–100 线性映射到 0–12 | 上限 12 |
| 人工加权 | 仅 `boost` 覆盖，封顶 | 0–15 |

所有建议项都带 `breakdown`（各维度明细分、`slackHours`、`ageDays`），可直接在 UI 解释「为什么这封排前面」。

评分只依赖信件静态属性与游戏状态，**不依赖装入顺序**，避免顺序相关的不稳定性。载重/航时差异不进评分，由硬闸门 + 到达时刻优选处理。

### 确定性排序键

所有候选信件按以下字典序排序（无随机、无 Map 遍历序依赖）：

```
(-总分, -加急等级, 截止时刻, -积压天数, 信件ID)
```

- 分数相同：加急高的在前；
- 仍相同：截止窗近的在前；
- 仍相同：积压更久的在前；
- 全部相同：信件 ID 升序——这是最终、完全确定的并列打破。

输出也全部排序：建议按 `(courierId, order, letterId)`，暂缓/未分配/拒绝各有固定排序。配置变更会改变 `configFingerprint`（FNV-1a 哈希），便于核对「同一输入」是否同参数。

## 3. 分配流程

`buildSortPlan(state, { windows, ledger, config })`：

1. **规范化输入**：窗口按信使建索引（重复/非法即报错）；台账重放为生效覆盖；只取 `inbox/backlog` 信件。
2. **建评分视图**：每封信算基础分；带 `hold` 的直接进 `held`（同时挂起该信其他覆盖）；`boost` 超限立即登记拒绝。
3. **确定性排名**：按排序键得到全局排名（`rank` 从 0 开始）。
4. **贪心装入**（按排名顺序，因此高优先级先占资源）：
   - `pin`：只评估指定信使；可行即装入，不可行拒绝且不改派；
   - 自动：对全部信使做可行性评估（窗口+目的地+数量+载重+航时），在可行集合中选**预计到达最早**，到达时刻并列按信使 ID；
   - 全不可行：进 `unassigned`，原因推断顺序固定（先窗口、再数量、再载重）。
5. **输出** `{ engineVersion, currentHour, windows, configFingerprint, assignments, held, unassigned, refusedOverrides }`。

航时可行性直接调用主引擎 `calculateRoute`，因此**分拣引擎的建议与实际结算口径一致**（顺风、雾礁 0.35h、曦光晨雾、风翎末段减速等全部继承）。

## 4. 人工覆盖台账（只追加、分层可撤销）

台账是事件数组，永不修改业务事实：

```js
{ id: 'OV-0001', seq: 0, kind: 'apply',
  letterId, action: 'pin'|'boost'|'hold',
  courierId, points, reason, note, createdAtHour,
  revoked: false, revokedByEventId: null }

{ id: 'OV-0002', seq: 1, kind: 'revoke', targetEventId: 'OV-0001', ... }
```

语义：

- **三种动作**：`pin`（指定信使，不改变排名）、`boost`（加 ≤15 分）、`hold`（今日不排）。不同动作可叠加在同一封信上；`hold` 优先级最高，生效时挂起其余覆盖（`held.audit.suppressedOverrides` 可查）。
- **同动作后者取代前者**：对同一封信重复 `boost`，新值生效、旧值进 `supersededEventIds`，但两条记录都保留。
- **撤销是追加 `revoke` 事件**，不是删除；被撤销的最新一条失效后，**上一条同动作覆盖自动恢复生效**（分层撤销）。
- **撤销本身也永久留痕**，可以审计「谁在什么时候以什么理由撤了什么」。
- 重复撤销同一事件直接报错（`已经被撤销`）。
- 非法参数（空信件 ID、pin 缺信使、boost 非正整数、未知动作）在写入台账时即抛错，不产生脏事件。

`activeOverrides(ledger)` 是纯重放：只依赖事件序列，不修改入参；兼容持久化数据上直接带 `revoked` 标记的形态。

## 5. 接口

| 方法/路径 | 说明 |
| --- | --- |
| `GET /api/game/sort-plan` | 只读，返回 `{ plan, ledger, assignments }`，`assignments` 可直接喂给现有 `plan/preview` 与 `day/advance` |
| `POST /api/game/overrides` | 追加覆盖（body：`letterId/action/courierId?/points?/reason?/note?`），返回事件、台账与最新快照 |
| `POST /api/game/overrides/revoke` | 追加撤销（body：`eventId/reason?`），返回事件、台账与最新快照 |

覆盖写操作走 `store.mutate`（原子写盘、乐观锁 revision 随主状态一起递增），台账保存在 `state.priorityLedger`，存档结构校验与损坏迁移已覆盖该字段。终局后三个接口都返回「本局已结束」。

## 6. 稳定性验证

- 同一 `state + windows + ledger + config` 连续调用、以及对窗口/台账做乱序重排后调用，结果 `deepEqual`（测试 1、2）。
- 完全等价信件按 ID 稳定排序（测试 4）。
- 42 项测试覆盖：硬闸门（窗口/载重/数量/目的地）、三种覆盖、分层撤销、撤销留痕、超限拒绝、接口持久化与重启恢复；`npm run check`（测试 + 客户端构建）全绿。
